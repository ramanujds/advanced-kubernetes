apiVersion: v1
kind: Pod
metadata:
    name: nginx
    labels:
      app: part-inventory-service
spec:
  containers:
   - name: nginx
     image: nginx
     ports:
      - containerPort: 80
     imagePullPolicy: Never
